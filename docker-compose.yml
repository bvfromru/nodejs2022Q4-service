services:
  app:
    build: .
    ports:
      - ${PORT}:${PORT}
    volumes:
      - ./src:/usr/app/src
    depends_on:
      - postgres
    environment:
      - PORT=${PORT}
    restart: on-failure
  postgres:
    build: ./postgreSQL_db
    ports:
      - 3001:3001
    restart: on-failure
